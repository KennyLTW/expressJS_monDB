import Button from '@mui/material/Button';
import React, { useState } from "react";

function Index() {
    return (

            <Button onPress={getInbox}>Get Inbox</Button>
    
    );

}

export default Index;